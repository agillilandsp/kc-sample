server:
  servletPath: /*
  port: 8080

spring:
  datasource:
    username: sa
    url: jdbc:h2:mem:customdb

keycloak:
  server:
    servletPath: /*
    contextPath: /auth
    adminUser:
      username: admin
      password: pass
#    realmImportFile: file-to-import